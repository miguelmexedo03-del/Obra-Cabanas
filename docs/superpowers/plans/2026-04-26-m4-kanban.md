# M4 — Kanban Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir um quadro Kanban drag-and-drop com 4 colunas (Por Fazer / Em Curso / Bloqueado / Concluído) que representa as 264 `tarefas_gantt` de nivel=2 (11 fases × 24 APs), com atualização optimista do status ao arrastar.

**Architecture:** `kanban_cards` VIEW (já existe) fornece os dados. `@dnd-kit/core` gere drag-and-drop com `useDraggable` nos cartões e `useDroppable` nas colunas. `useOptimistic` + `useTransition` garantem feedback imediato com rollback automático se o servidor falhar. O Server Action `updateTarefa` de M3 é reutilizado — já aceita `{ status }`.

**Tech Stack:** Next.js 15 App Router, TypeScript, Tailwind CSS, shadcn/ui, `@dnd-kit/core`, `@dnd-kit/utilities`, `useOptimistic`, `useTransition`, `updateTarefa` (Server Action existente).

---

## Ficheiros

**Novos:**
- `src/components/kanban/kanban-card.tsx` — cartão individual + tipo `KanbanCardData`
- `src/components/kanban/kanban-column.tsx` — coluna droppable
- `src/components/kanban/kanban-board.tsx` — board container com `DndContext` + `useOptimistic`
- `src/app/(app)/kanban/page.tsx` — Server Component que busca dados e renderiza o board
- `e2e/kanban.spec.ts` — teste e2e

**Modificados:**
- `src/app/(app)/layout.tsx` — adicionar link "Kanban" à nav

**Não modificados (reutilizados):**
- `src/app/actions/gantt.ts` — `updateTarefa` aceita `{ status }` e já existe
- `src/lib/validations/gantt.ts` — `UpdateTarefaInput` já tem o campo `status`

---

## Task 0 — Instalar @dnd-kit

**Files:**
- Modify: `package.json` (via npm install)

- [ ] **Step 1: Instalar pacotes**

```bash
cd "C:\Users\migue\Desktop\Obra Cabanas\Planeamento do calendario\obra-cabanas-app" && npm install @dnd-kit/core @dnd-kit/utilities @dnd-kit/sortable
```

Esperado: 3 pacotes instalados sem peer dependency errors. React 19 é compatível.

- [ ] **Step 2: Verificar instalação**

```bash
cd "C:\Users\migue\Desktop\Obra Cabanas\Planeamento do calendario\obra-cabanas-app" && npm list @dnd-kit/core @dnd-kit/utilities @dnd-kit/sortable 2>&1 | head -10
```

Esperado: versões listadas sem `UNMET PEER DEPENDENCY`.

---

## Task 1 — `KanbanCard` component

**Files:**
- Create: `src/components/kanban/kanban-card.tsx`

Este é o componente-folha. Define o tipo `KanbanCardData` que será importado pelos outros componentes. Usa `useDraggable` para drag com Pointer Events (funciona em mobile). `CSS.Transform.toString` converte o objeto `transform` de dnd-kit para CSS.

- [ ] **Step 1: Criar componente**

```tsx
// src/components/kanban/kanban-card.tsx
'use client'

import { useDraggable } from '@dnd-kit/core'
import { CSS } from '@dnd-kit/utilities'
import { format, parseISO } from 'date-fns'
import { pt } from 'date-fns/locale'
import { Progress } from '@/components/ui/progress'
import { cn } from '@/lib/utils'

export interface KanbanCardData {
  id: number
  apartamento_codigo: string
  fase_nome: string
  fase_cor: string
  inicio: string | null
  fim: string | null
  status: string
  responsavel_nome: string | null
  progresso: number
}

interface KanbanCardProps {
  card: KanbanCardData
  canEdit: boolean
}

export function KanbanCard({ card, canEdit }: KanbanCardProps) {
  const { attributes, listeners, setNodeRef, transform, isDragging } = useDraggable({
    id: card.id,
    data: { card },
    disabled: !canEdit,
  })

  const style = transform ? { transform: CSS.Transform.toString(transform) } : undefined

  return (
    <div
      ref={setNodeRef}
      style={style}
      {...(canEdit ? listeners : {})}
      {...(canEdit ? attributes : {})}
      className={cn(
        'rounded-lg border bg-card p-3 shadow-sm select-none transition-shadow',
        canEdit && 'cursor-grab active:cursor-grabbing hover:shadow-md',
        isDragging && 'opacity-40 shadow-none',
        !canEdit && 'cursor-default',
      )}
    >
      <div className="flex items-start gap-2">
        <div
          className="w-1 self-stretch rounded-full shrink-0 mt-0.5"
          style={{ backgroundColor: card.fase_cor }}
        />
        <div className="flex-1 min-w-0">
          <p className="text-sm font-medium truncate leading-tight">{card.fase_nome}</p>
          <p className="text-xs text-muted-foreground font-mono mt-0.5">{card.apartamento_codigo}</p>

          {(card.inicio || card.fim) && (
            <p className="text-xs text-muted-foreground mt-1.5">
              {card.inicio
                ? format(parseISO(card.inicio), 'd MMM', { locale: pt })
                : '?'}
              {' → '}
              {card.fim
                ? format(parseISO(card.fim), 'd MMM', { locale: pt })
                : '?'}
            </p>
          )}

          {card.responsavel_nome && (
            <p className="text-xs text-muted-foreground mt-1 truncate">{card.responsavel_nome}</p>
          )}
        </div>
      </div>

      <div className="mt-2.5">
        <div className="flex items-center justify-between mb-1">
          <span className="text-xs text-muted-foreground">{Math.round(card.progresso * 100)}%</span>
        </div>
        <Progress value={card.progresso * 100} className="h-1" />
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Verificar build parcial**

```bash
cd "C:\Users\migue\Desktop\Obra Cabanas\Planeamento do calendario\obra-cabanas-app" && npx tsc --noEmit 2>&1 | grep -i "kanban-card" | head -10
```

Esperado: sem erros relacionados com `kanban-card.tsx`.

---

## Task 2 — `KanbanColumn` component

**Files:**
- Create: `src/components/kanban/kanban-column.tsx`

A coluna é o `useDroppable` target. O `isOver` flag aplica highlight visual quando uma carta passa por cima. O `ref` abrange toda a coluna (incluindo as cartas dentro) — por isso arrastar sobre uma carta existente ainda conta como "sobre a coluna".

- [ ] **Step 1: Criar componente**

```tsx
// src/components/kanban/kanban-column.tsx
'use client'

import { useDroppable } from '@dnd-kit/core'
import { cn } from '@/lib/utils'
import { KanbanCard, type KanbanCardData } from './kanban-card'

const COLUMN_LABELS: Record<string, string> = {
  por_fazer: 'Por Fazer',
  em_curso: 'Em Curso',
  bloqueado: 'Bloqueado',
  concluido: 'Concluído',
}

const COLUMN_HEADING_COLORS: Record<string, string> = {
  por_fazer: 'text-muted-foreground',
  em_curso: 'text-blue-600 dark:text-blue-400',
  bloqueado: 'text-red-600 dark:text-red-400',
  concluido: 'text-green-600 dark:text-green-400',
}

interface KanbanColumnProps {
  status: string
  cards: KanbanCardData[]
  canEdit: boolean
}

export function KanbanColumn({ status, cards, canEdit }: KanbanColumnProps) {
  const { setNodeRef, isOver } = useDroppable({ id: status })

  return (
    <div className="flex flex-col min-h-0">
      {/* Column header */}
      <div className="flex items-center gap-2 mb-3 px-1">
        <h2 className={cn('text-sm font-semibold', COLUMN_HEADING_COLORS[status])}>
          {COLUMN_LABELS[status]}
        </h2>
        <span className="text-xs bg-muted rounded-full px-2 py-0.5 text-muted-foreground tabular-nums">
          {cards.length}
        </span>
      </div>

      {/* Drop zone */}
      <div
        ref={setNodeRef}
        className={cn(
          'flex-1 space-y-2 rounded-lg p-2 min-h-48 transition-colors duration-150',
          isOver && canEdit
            ? 'bg-primary/5 ring-2 ring-primary/30'
            : 'bg-muted/30',
        )}
      >
        {cards.map(card => (
          <KanbanCard key={card.id} card={card} canEdit={canEdit} />
        ))}

        {cards.length === 0 && (
          <div className={cn(
            'flex items-center justify-center h-24 rounded-md border-2 border-dashed text-xs text-muted-foreground/50 transition-colors',
            isOver && canEdit && 'border-primary/40 text-primary/60',
          )}>
            Sem cartões
          </div>
        )}
      </div>
    </div>
  )
}
```

---

## Task 3 — `KanbanBoard` component

**Files:**
- Create: `src/components/kanban/kanban-board.tsx`

O board é o componente mais complexo. Contém:
- `DndContext` com `PointerSensor` (mouse + touch) e `TouchSensor` separado para mobile
- `useOptimistic` para mover a carta imediatamente (sem esperar pelo servidor)
- `useTransition` para envolver o Server Action
- `DragOverlay` para mostrar ghost card enquanto arrasta
- `handleDragEnd`: se `over.id` for um status válido diferente do atual, atualiza optimisticamente e persiste via `updateTarefa`

```tsx
// src/components/kanban/kanban-board.tsx
'use client'

import { useState, useOptimistic, useTransition } from 'react'
import {
  DndContext,
  DragOverlay,
  PointerSensor,
  TouchSensor,
  useSensor,
  useSensors,
  type DragEndEvent,
  type DragStartEvent,
} from '@dnd-kit/core'
import { KanbanColumn } from './kanban-column'
import { KanbanCard, type KanbanCardData } from './kanban-card'
import { updateTarefa } from '@/app/actions/gantt'

const STATUSES = ['por_fazer', 'em_curso', 'bloqueado', 'concluido'] as const
type Status = typeof STATUSES[number]

interface KanbanBoardProps {
  cards: KanbanCardData[]
  canEdit: boolean
}
```

- [ ] **Step 1: Criar componente completo**

```tsx
// src/components/kanban/kanban-board.tsx
'use client'

import { useState, useOptimistic, useTransition } from 'react'
import {
  DndContext,
  DragOverlay,
  PointerSensor,
  TouchSensor,
  useSensor,
  useSensors,
  type DragEndEvent,
  type DragStartEvent,
} from '@dnd-kit/core'
import { KanbanColumn } from './kanban-column'
import { KanbanCard, type KanbanCardData } from './kanban-card'
import { updateTarefa } from '@/app/actions/gantt'

const STATUSES = ['por_fazer', 'em_curso', 'bloqueado', 'concluido'] as const
type Status = typeof STATUSES[number]

interface KanbanBoardProps {
  cards: KanbanCardData[]
  canEdit: boolean
}

export function KanbanBoard({ cards: initialCards, canEdit }: KanbanBoardProps) {
  const [, startTransition] = useTransition()
  const [optimisticCards, updateOptimisticCards] = useOptimistic(
    initialCards,
    (state: KanbanCardData[], { id, status }: { id: number; status: string }) =>
      state.map(c => (c.id === id ? { ...c, status } : c)),
  )
  const [activeCard, setActiveCard] = useState<KanbanCardData | null>(null)
  const [dragError, setDragError] = useState<string | null>(null)

  const sensors = useSensors(
    useSensor(PointerSensor, { activationConstraint: { distance: 5 } }),
    useSensor(TouchSensor, { activationConstraint: { delay: 250, tolerance: 5 } }),
  )

  const cardsByStatus = Object.fromEntries(
    STATUSES.map(s => [s, optimisticCards.filter(c => c.status === s)]),
  ) as Record<Status, KanbanCardData[]>

  function handleDragStart(event: DragStartEvent) {
    const card = optimisticCards.find(c => c.id === event.active.id)
    setActiveCard(card ?? null)
    setDragError(null)
  }

  function handleDragEnd(event: DragEndEvent) {
    setActiveCard(null)
    const { active, over } = event
    if (!over || !canEdit) return

    const cardId = active.id as number
    const newStatus = over.id as string

    if (!STATUSES.includes(newStatus as Status)) return

    const card = optimisticCards.find(c => c.id === cardId)
    if (!card || card.status === newStatus) return

    startTransition(async () => {
      updateOptimisticCards({ id: cardId, status: newStatus })
      const result = await updateTarefa(cardId, { status: newStatus as Status })
      if (!result.success) {
        setDragError(`Erro ao guardar: ${result.error}`)
        setTimeout(() => setDragError(null), 4000)
      }
    })
  }

  return (
    <div className="space-y-3">
      {dragError && (
        <p role="alert" className="text-sm text-destructive bg-destructive/10 px-3 py-2 rounded-md">
          {dragError}
        </p>
      )}

      <DndContext sensors={sensors} onDragStart={handleDragStart} onDragEnd={handleDragEnd}>
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 items-start">
          {STATUSES.map(status => (
            <KanbanColumn
              key={status}
              status={status}
              cards={cardsByStatus[status]}
              canEdit={canEdit}
            />
          ))}
        </div>

        <DragOverlay dropAnimation={null}>
          {activeCard && <KanbanCard card={activeCard} canEdit={false} />}
        </DragOverlay>
      </DndContext>
    </div>
  )
}
```

- [ ] **Step 2: Verificar types**

```bash
cd "C:\Users\migue\Desktop\Obra Cabanas\Planeamento do calendario\obra-cabanas-app" && npx tsc --noEmit 2>&1 | grep -i "kanban" | head -15
```

Esperado: sem erros de TypeScript nos ficheiros kanban.

---

## Task 4 — Página `/kanban` + link na nav

**Files:**
- Create: `src/app/(app)/kanban/page.tsx`
- Modify: `src/app/(app)/layout.tsx`

A página é um Server Component que busca da `kanban_cards` view. Seleciona apenas as colunas necessárias para não transferir dados desnecessários. Determina `canEdit` baseado no `role` do perfil, tal como as outras páginas.

- [ ] **Step 1: Criar página**

```tsx
// src/app/(app)/kanban/page.tsx
import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { KanbanBoard } from '@/components/kanban/kanban-board'

export default async function KanbanPage() {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  const [{ data: cards }, { data: profile }] = await Promise.all([
    supabase
      .from('kanban_cards')
      .select('id, apartamento_codigo, fase_nome, fase_cor, inicio, fim, status, responsavel_nome, progresso')
      .order('apartamento_id')
      .order('fase_id'),
    supabase.from('profiles').select('role').eq('id', user.id).single(),
  ])

  const canEdit = profile?.role === 'admin' || profile?.role === 'encarregado'

  return (
    <div className="space-y-4">
      <div className="flex items-center justify-between">
        <h1 className="text-xl font-semibold">Kanban — Obra Cabanas</h1>
        <p className="text-sm text-muted-foreground">
          {canEdit ? 'Arrasta os cartões para mudar de estado.' : 'Só leitura.'}
        </p>
      </div>

      <KanbanBoard cards={cards ?? []} canEdit={canEdit} />
    </div>
  )
}
```

- [ ] **Step 2: Adicionar link na nav**

Em `src/app/(app)/layout.tsx`, na linha onde está o array `navLinks`, adicionar a entrada para Kanban:

```tsx
const navLinks = [
  { href: '/', label: 'Dashboard' },
  { href: '/apartamentos', label: 'Apartamentos' },
  { href: '/checklist', label: 'Checklist' },
  { href: '/gantt', label: 'Gantt' },
  { href: '/kanban', label: 'Kanban' },   // ← adicionar esta linha
]
```

---

## Task 5 — Build completo

**Files:** nenhum novo — apenas verificação

- [ ] **Step 1: Build**

```bash
cd "C:\Users\migue\Desktop\Obra Cabanas\Planeamento do calendario\obra-cabanas-app" && npm run build 2>&1 | tail -30
```

Esperado: `✓ Compiled successfully` (ou similar Next.js 16 output). Zero erros TypeScript.

Se aparecer erro de tipo no `updateTarefa` relacionado com `status`:
- O campo `status` em `updateTarefaSchema` é `z.enum(['por_fazer', 'em_curso', 'bloqueado', 'concluido']).optional()`
- O cast `newStatus as Status` resolve. Se o TypeScript reclamar, confirmar que `Status` e o enum do zod têm os mesmos valores.

Se aparecer erro `Cannot find module '@dnd-kit/core'`:
- Confirmar que o `npm install` do Task 0 correu com sucesso.

---

## Task 6 — E2E test + commit

**Files:**
- Create: `e2e/kanban.spec.ts`

- [ ] **Step 1: Criar teste e2e**

```ts
// e2e/kanban.spec.ts
import { test, expect } from '@playwright/test'

const EMAIL = process.env.TEST_USER_EMAIL ?? ''
const PASSWORD = process.env.TEST_USER_PASSWORD ?? ''

test.skip(!EMAIL || !PASSWORD, 'Set TEST_USER_EMAIL and TEST_USER_PASSWORD in .env.test.local')

async function login(page: import('@playwright/test').Page) {
  await page.goto('/login')
  await page.getByLabel('Email').fill(EMAIL)
  await page.getByLabel('Password').fill(PASSWORD)
  await page.getByRole('button', { name: /entrar/i }).click()
  await page.waitForURL('/')
}

test('kanban page renders 4 columns', async ({ page }) => {
  await login(page)
  await page.goto('/kanban')
  await page.waitForLoadState('networkidle')

  await expect(page.getByRole('heading', { name: /kanban/i })).toBeVisible()
  await expect(page.getByText('Por Fazer')).toBeVisible()
  await expect(page.getByText('Em Curso')).toBeVisible()
  await expect(page.getByText('Bloqueado')).toBeVisible()
  await expect(page.getByText('Concluído')).toBeVisible()
})

test('kanban nav link is present', async ({ page }) => {
  await login(page)
  await expect(page.getByRole('link', { name: /kanban/i })).toBeVisible()
})

test('kanban shows cards with AP codes', async ({ page }) => {
  await login(page)
  await page.goto('/kanban')
  await page.waitForLoadState('networkidle')

  // Deve haver pelo menos um card com código AP (AP1..AP24)
  const firstApCard = page.getByText(/^AP\d+$/).first()
  await expect(firstApCard).toBeVisible({ timeout: 5000 })
})
```

- [ ] **Step 2: Correr teste**

```bash
cd "C:\Users\migue\Desktop\Obra Cabanas\Planeamento do calendario\obra-cabanas-app" && npm run test:e2e -- e2e/kanban.spec.ts 2>&1 | tail -20
```

Esperado: 3 testes passam (ou "skip" se `.env.test.local` não existir).

- [ ] **Step 3: Commit**

```bash
cd "C:\Users\migue\Desktop\Obra Cabanas\Planeamento do calendario\obra-cabanas-app" && git add src/components/kanban/ src/app/\(app\)/kanban/ src/app/\(app\)/layout.tsx e2e/kanban.spec.ts package.json package-lock.json && git commit -m "feat: M4 — Kanban com drag-and-drop entre colunas de estado"
```

---

## Verificação end-to-end

1. `/kanban` carrega sem erro 500
2. 4 colunas visíveis: Por Fazer / Em Curso / Bloqueado / Concluído
3. "Por Fazer" tem os 264 cartões (todos começam neste estado)
4. Arrastar um cartão para "Em Curso" → cartão move imediatamente (optimistic)
5. Recarregar página → cartão ainda está em "Em Curso" (persistiu no servidor)
6. Arrastar de volta para "Por Fazer" → funciona
7. Em mobile (ou DevTools responsive): arrasto com touch funciona (TouchSensor ativo)
8. `npm run build` sem erros TypeScript
9. `npm run test:e2e e2e/kanban.spec.ts` → 3 passes

---

## Notas técnicas

**Porquê `useDroppable` nas colunas e não `SortableContext`?**
O status de uma tarefa não tem "posição" — é um enum com 4 valores. Sortable seria necessário se quiséssemos ordenar cartões dentro de uma coluna, o que não é um requisito. `useDroppable` é suficiente e mais simples.

**Porquê `dropAnimation={null}` no `DragOverlay`?**
Sem esta prop, quando se larga um cartão, o overlay anima de volta à posição original por um curto instante, criando um flash visual. `null` desativa a animação de saída.

**`activationConstraint: { distance: 5 }` no `PointerSensor`:**
Sem este constraint, clicar num cartão aciona o drag imediatamente. Com `distance: 5`, o drag só começa se o ponteiro se mover 5px — distingue click de drag.

**`useOptimistic` reverte automaticamente:**
Se `updateTarefa` falhar (retorna `{ success: false }`), o estado optimista reverte para `initialCards` automaticamente quando a `useTransition` termina. Não é necessário rollback manual — o React trata disso.
